import { BaseScreen } from "@/core/component/base-screen.component";

export class AboutAs extends BaseScreen{
    constructor(){
        super({title: "About"})
    }

    render(){
        return '<p>AboutAs</p>'
    }
}